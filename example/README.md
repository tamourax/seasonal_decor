# seasonal_decor_example

This example includes two entrypoints:

- `lib/main.dart` (simple demo)
- `lib/advanced_main.dart` (full controls)

Run the advanced demo:

```bash
flutter run -t lib/advanced_main.dart
```
