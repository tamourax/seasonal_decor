name: seasonal_decor
description: Drop-in seasonal decorative overlays for Flutter apps with a built-in particle engine.
version: 0.1.0
homepage: https://github.com/tamourax/seasonal_decor
repository: https://github.com/tamourax/seasonal_decor
issue_tracker: https://github.com/tamourax/seasonal_decor/issues
topics:
  - animations
  - ramadan
  - seasonal
  - overlay
  - celebration

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
